@model IEnumerable<NmpOnTap.Models.NmpTacGia>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_NmpLayout.cshtml";
}

<h2>Danh sách tác giả</h2>

<p>
    @Html.ActionLink("Create New", "NmpCreate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nmp_TenTG)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nmp_TenTG)
        </td>
        <td>
            @Html.ActionLink("Edit", "NmpEdit", new { id=item.Nmp_MaTG }) |
            @Html.ActionLink("Details", "NmpDetails", new { id=item.Nmp_MaTG }) |
            @Html.ActionLink("Delete", "NmpDelete", new { id=item.Nmp_MaTG })
        </td>
    </tr>
}

</table>
